"""
Gensim 是什么？通俗易懂讲明白
一、一句话
Gensim 是 Python 专门做「文本语义、主题模型、词向量」的 NLP 工具库，主打无监督文本建模、词向量训练、相似度计算。
全称：Generate Similarity（生成相似度）
二、核心定位
专门处理：
海量文本
无监督训练
词向量 Word2Vec
主题模型 LDA/LSI
文本相似度、检索
轻量化、效率高、适合大规模语料。
三、你用到的 KeyedVectors 是干嘛的
就是 专门加载、使用预训练词向量 的模块：
加载 word2vec bin/txt 词向量
查词语义相似度
找近义词、词加减运算
计算向量模长、余弦相似度
四、Gensim 最常用能干啥
训练 Word2Vec 词向量
把词语变成稠密向量（就是你刚才问的稠密向量、向量模长）
加载别人训练好的词向量
就是你现在 from gensim.models import KeyedVectors 干的事
LDA 主题建模
自动把一堆文章分出主题
文本相似度匹配、语义检索
支持流式大数据，不用把所有文本一次性读进内存
五、和其他库区别
NLTK：偏向分词、词性标注、基础文本处理
Jieba：专门中文分词
Gensim：偏向词向量、语义、主题、相似度
"""